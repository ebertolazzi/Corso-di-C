#include <stdio.h>
#define NUM 10
#define FUN(X) X*X
#define FOR(X,Y) \
for(int i = 0; i < X; i++){\
    Y\
}
#define ESEGUI(F,A,B) AFB
#define AFB 10


// #define TEXT \
// printf("rerere");\
// printf("fdfdfdf");

int main(){
    int vett[NUM];
    int vatt2[NUM];
    int r = FUN(4);
    FOR(10,r+=i;)
    int g = ESEGUI(+,4,5);
}