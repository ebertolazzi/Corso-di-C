

#include <stdio.h>

int main(){
    int r = 3;
    /* Cosa non bisogna evitare per non rischiare di avere un codice illeggibile */
    printf("--r+3/-3.0==0||1 => %d\n",--r+3/-3.0==0||1);
    /* Cosa non bisogna evitare per non rischiare di avere un codice illeggibile */
    printf("3!=1==-sizeof(3)/-2*2 => %d\n",3!=1==-sizeof(3)/-2*2);
}